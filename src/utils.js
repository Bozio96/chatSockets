//Este archivo nos permite trabajar con esModules( import from )

/* import { fileURLToPath } from "url";
import {dirname} from 'path';
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

export default __dirname; */ 
//DEFINICIOS: El dirname nos da la ruta desde la raiz del disco duro, hasta la carpeta que contiene al archivo donde se ejecutó